<p id="notice"><%= notice %></p>
<h3>Review for <%= @movie.title %><em> (<%= @movie.year %>)</em></h3>
<p>
  <strong>Review:</strong>
  <%= @review.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @review.content %>
</p>

<p>
  <strong>Reviewer's Rating:</strong>
  <%= number_with_precision(@review.rating, precision: 1)  %>
</p>
<% if current_user.id == @review.user_id %>
<%= link_to 'Edit', edit_movie_review_path(@movie,@review) %><br/>
<% end %>
<%= link_to 'Back to Movie page', @movie %><br/>
<%= link_to 'Back to Main Page - Movies List', movies_path %>
